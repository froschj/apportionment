/*
 * text interface to apportion seats specified in a csv. 
 */

#include "../src/State.hpp"
#include "../src/methods.hpp"
#include "../src/Apportionment.hpp"

#include <iostream>

bool parseArgs(int numArgs, char **args, unsigned &seats);

int main(int argc, const char** argv) {

    return 0;
}

//checks for the correct number of arguments. return true if usage is correct
//if usage is correct
bool checkUsage(int numArgs, char **args, unsigned &seats) {

}